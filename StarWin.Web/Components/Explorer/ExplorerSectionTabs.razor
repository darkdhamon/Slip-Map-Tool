<nav class="section-tabs" aria-label="StarWin sections">
    @foreach (var sectionName in Sections)
    {
        var sectionUrl = SectionHrefFactory?.Invoke(sectionName) ?? "#";
        <a href="@sectionUrl"
           class="@(ActiveSection == sectionName ? "active" : null)"
           onclick="return window.starforgedAtlasNavigation.showSectionRouteLoading(event, '@sectionUrl', '@sectionName');"
           aria-current="@(ActiveSection == sectionName ? "page" : null)">
            @sectionName
        </a>
    }
</nav>
@code {
    [Parameter]
    public IReadOnlyList<string> Sections { get; set; } = [];

    [Parameter]
    public string ActiveSection { get; set; } = string.Empty;

    [Parameter]
    public bool IsLoading { get; set; }

    [Parameter]
    public string LoadingTarget { get; set; } = string.Empty;

    [Parameter]
    public string LoadingStatus { get; set; } = string.Empty;

    [Parameter]
    public Func<string, string>? SectionHrefFactory { get; set; }
}
