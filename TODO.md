# TODO

- Implement a dedicated ship system for Starforged Atlas.
- Add ship-specific FTL distance configuration on a dedicated ship screen rather than only sector settings.
- Add ship-specific route planner preferences on a dedicated ship screen.
- Add ship-specific avoided empires and avoided systems on a dedicated ship screen.
- Add ship-specific travel modifiers later if route timing or vessel capabilities need per-ship overrides.
